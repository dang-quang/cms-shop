import {
  defaultFont, grayColor, primaryBoxShadow, primaryColor,
  whiteColor
} from "assets/jss/natcash.js";

const style = {
  form_control: {
    width: "100%",
  },
  input_label: {
    backgroundColor: whiteColor,
    padding: '0 5px'
  },
};

export default style;
